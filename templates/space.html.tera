<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sticky Notes</title>
    <link rel="stylesheet" href="/css/space.css">
</head>
<body class="bg-gray-200">
    <div class="container mx-auto p-6">
        <h1 class="text-2xl font-bold mb-4">My Sticky Notes</h1>
        <div id="notes-container" class="relative">
            {% if notes %}
            <ul>
                {% for note in notes %}
                    <li>{{ note.text }}</li>
                {% endfor %}
            </ul>
        {% else %}
            <p>No sticky notes available.</p>
        {% endif %}          
        </div>
        <button onclick="addNewNote()" class="mt-4 px-4 py-2 bg-blue-500 text-white rounded">Add New Note</button>
    </div>

    <script src="/js/space.js"></script>
</body>
</html>
